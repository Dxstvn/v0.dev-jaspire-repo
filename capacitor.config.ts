import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dustinjasmin.cashback',
  appName: 'Jaspire Cashback Investments',
  webDir: 'out'
};

export default config;
